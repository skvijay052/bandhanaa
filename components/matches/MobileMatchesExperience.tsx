"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { BadgeCheck,Bell,Bookmark,BriefcaseBusiness,Eye,GraduationCap,Heart,MapPin,Ruler,Send,UsersRound,X } from "lucide-react";
import { Brand } from "@/components/auth/Brand";
import { ProfileImage } from "@/components/ui/ProfileImage";
import type { MatchProfile,MatchTab } from "@/data/matches";
type MobileTabCounts=Record<MatchTab,number>;
const mobileTabs:Array<{id:MatchTab;label:string;icon:typeof UsersRound}>=[{id:"all",label:"All Matches",icon:UsersRound},{id:"shortlisted",label:"Shortlisted",icon:Bookmark},{id:"sent",label:"Sent",icon:Send},{id:"received",label:"Received",icon:Heart}];
export function MobileMatchesExperience({profiles,shortlisted,sentIds,followingIds,activeTab,tabCounts,onTabChange,onShortlist,onInterest,onRemoveRelationship}:{profiles:MatchProfile[];shortlisted:string[];sentIds:string[];followingIds:string[];activeTab:MatchTab;tabCounts:MobileTabCounts;onTabChange:(tab:MatchTab)=>void;onShortlist:(profile:MatchProfile)=>void;onInterest:(profile:MatchProfile)=>void;onRemoveRelationship:(profile:MatchProfile)=>void;}){const orderedProfiles=[...profiles].sort((a,b)=>b.compatibility-a.compatibility);return <div className="mobile-half-type mobile-matches-type relative z-10 px-4 md:hidden"><header className="sticky top-0 z-[90] -mx-4 grid grid-cols-[40px_1fr_40px] items-center border-b border-black/5 bg-[#f8fafc]/95 px-4 py-3 backdrop-blur-xl"><Link href="/discover"><Brand compact/></Link><span/><Link href="/notifications" aria-label="Notifications" className="relative grid size-9 place-items-center rounded-[13px] bg-white shadow-[0_8px_24px_rgba(44,33,80,.1)]"><Bell size={17}/><span className="absolute right-1.5 top-1.5 size-1.5 rounded-full bg-[#f34ca4] ring-2 ring-white"/></Link></header><nav className="-mx-4 flex gap-2.5 overflow-x-auto bg-[#f8fafc] px-4 py-3.5 [scrollbar-width:none]">{mobileTabs.map(({id,label,icon:Icon})=>{const active=activeTab===id;return <button key={id} onClick={()=>onTabChange(id)} className={`flex h-11 shrink-0 items-center gap-2 rounded-full px-4 text-[13px] font-semibold ${active?"bg-gradient-to-r from-[#7b35ff] via-[#bd42e4] to-[#f54fa8] text-white shadow-[0_8px_24px_rgba(153,60,231,.24)]":"border border-[#ece8f0] bg-white text-[#171a22] shadow-[0_5px_18px_rgba(44,33,80,.07)]"}`}><Icon size={17}/>{label}<span className={`grid h-7 min-w-7 place-items-center rounded-full px-2 text-[11px] font-bold ${active?"bg-white/92 text-[#8b3de8]":"bg-[#f0f1f5] text-[#596172]"}`}>{tabCounts[id]}</span></button>})}</nav>{orderedProfiles.length?<div className="-mx-4 h-[calc(100dvh-194px)] snap-y snap-mandatory overflow-y-auto px-4 [scrollbar-width:none]">{orderedProfiles.map((profile,index)=><div key={profile.id} className="flex min-h-full snap-start items-start py-2"><MobileMatchCard profile={profile} liked={shortlisted.includes(profile.id)} sent={sentIds.includes(profile.id)} following={followingIds.includes(profile.id)} priority={index<2} onShortlist={()=>onShortlist(profile)} onInterest={()=>onInterest(profile)} onRemoveRelationship={()=>onRemoveRelationship(profile)}/></div>)}</div>:<div className="grid min-h-[52dvh] place-items-center px-6 text-center"><div><h2 className="text-[18px] font-semibold">{emptyTitle(activeTab)}</h2><p className="mt-2 text-[13px] text-[#687184]">{emptyMessage(activeTab)}</p></div></div>}</div>}
function MobileMatchCard({profile,liked,sent,following,priority,onShortlist,onInterest,onRemoveRelationship}:{profile:MatchProfile;liked:boolean;sent:boolean;following:boolean;priority:boolean;onShortlist:()=>void;onInterest:()=>void;onRemoveRelationship:()=>void;}){const[confirmAction,setConfirmAction]=useState<"sent"|"following"|null>(null);const[localSent,setLocalSent]=useState(sent);const[localFollowing,setLocalFollowing]=useState(following);useEffect(()=>setLocalSent(sent),[sent]);useEffect(()=>setLocalFollowing(following),[following]);function handleRelationship(){if(localFollowing){setConfirmAction("following");return}if(localSent){setConfirmAction("sent");return}setLocalSent(true);onInterest()}function confirmRemoval(){const action=confirmAction;setConfirmAction(null);if(action==="sent")setLocalSent(false);if(action==="following")setLocalFollowing(false);onRemoveRelationship()}return <article className="w-full overflow-hidden rounded-[20px] border border-[#eceaf0] bg-white shadow-[0_10px_30px_rgba(42,35,70,.09)]"><div className="relative h-[clamp(245px,43dvh,380px)] overflow-hidden bg-[#e8e9ec]"><Link href={`/profile/${profile.id}`} className="absolute inset-0"><ProfileImage src={profile.image} alt={profile.name} fill priority={priority} sizes="calc(100vw - 32px)" className="object-cover"/></Link><div className="absolute inset-0 bg-gradient-to-t from-black/75 via-transparent to-black/5"/><span className="absolute left-4 top-4 rounded-full bg-black/55 px-3 py-1.5 text-[11px] text-white">{profile.online?"Online":"Offline"}</span><button onClick={onShortlist} className="absolute right-4 top-4 grid size-10 place-items-center rounded-[12px] bg-white text-[#8b3de8] shadow"><Bookmark size={18} fill={liked?"currentColor":"none"}/></button><div className="absolute inset-x-5 bottom-4 text-white"><Link href={`/profile/${profile.id}`}><h2 className="flex items-center gap-1.5 text-[18px] font-semibold">{profile.name}, {profile.age||"Age hidden"}{profile.verified?<BadgeCheck size={16} className="fill-[#1d9bf0] text-[#1d9bf0]"/>:null}</h2></Link><p className="mt-1.5 text-[12px]">{profile.occupation}</p><p className="mt-1.5 flex items-center gap-1.5 text-[12px]"><MapPin size={14}/>{profile.city}, {profile.state}, India</p></div></div><div className="grid grid-cols-3 divide-x divide-[#eceaf0] border-b border-[#eceaf0] px-1 py-2.5"><MatchDetail icon={GraduationCap} label="Education" value={profile.education}/><MatchDetail icon={BriefcaseBusiness} label="Profession" value={profile.occupation}/><MatchDetail icon={Ruler} label="Height" value={profile.height}/></div><p className="line-clamp-2 min-h-[40px] px-4 py-2.5 text-[12px] leading-5 text-[#687184]">{profile.about||"No description added yet."}</p><div className="grid grid-cols-2 gap-2.5 border-t border-[#eceaf0] p-2.5"><Link href={`/profile/${profile.id}`} className="flex h-10 items-center justify-center gap-1.5 rounded-full border border-[#8b3de8] text-[11px] font-semibold text-[#8b3de8]"><Eye size={14}/>View Profile</Link><button type="button" onClick={handleRelationship} className="flex h-10 items-center justify-center gap-1.5 rounded-full bg-gradient-to-r from-[#a34cef] to-[#f45ca9] text-[11px] font-semibold text-white"><Heart size={14}/>{localFollowing?"Following":localSent?"Requested":"Send Request"}</button></div>{confirmAction?<div role="dialog" aria-modal="true" aria-labelledby={`match-confirm-${profile.id}`} className="fixed inset-0 z-[200] grid place-items-end bg-black/45 p-4 backdrop-blur-[2px]" onMouseDown={event=>event.target===event.currentTarget&&setConfirmAction(null)}><div className="w-full max-w-sm rounded-[26px] bg-white p-6 shadow-2xl"><div className="mx-auto grid size-12 place-items-center rounded-full bg-[#fff0f5] text-[#e72c6c]"><X size={22}/></div><h2 id={`match-confirm-${profile.id}`} className="mt-4 text-center text-[20px] font-bold">{confirmAction==="sent"?"Cancel request?":`Unfollow ${profile.name}?`}</h2><p className="mt-2 text-center text-[14px] leading-5 text-[#687184]">{confirmAction==="sent"?"Are you sure you want to cancel this request?":"You’ll stop following this profile."}</p><div className="mt-6 grid grid-cols-2 gap-3"><button type="button" onClick={()=>setConfirmAction(null)} className="h-12 rounded-full border border-[#d8d4dc] font-semibold">{confirmAction==="sent"?"Keep Request":"Keep Following"}</button><button type="button" onClick={confirmRemoval} className="h-12 rounded-full bg-[#e72c6c] font-semibold text-white">{confirmAction==="sent"?"Cancel Request":"Unfollow"}</button></div></div></div>:null}</article>}
function MatchDetail({icon:Icon,label,value}:{icon:typeof GraduationCap;label:string;value:string;}){return <div className="flex min-w-0 items-center gap-1.5 px-1.5"><span className="grid size-7 shrink-0 place-items-center rounded-full bg-[#f6efff] text-[#7651a8]"><Icon size={13}/></span><span className="min-w-0"><small className="block truncate text-[9px] text-[#8a91a1]">{label}</small><strong className="block truncate text-[10px] font-medium">{value}</strong></span></div>}
function emptyTitle(tab:MatchTab){if(tab==="shortlisted")return"No shortlisted profiles yet";if(tab==="sent")return"No requests sent yet";if(tab==="received")return"No requests received yet";return"No matches found"}function emptyMessage(tab:MatchTab){if(tab==="shortlisted")return"Profiles you save will appear here for quick access.";if(tab==="sent")return"Profiles you send a request to will appear here.";if(tab==="received")return"New requests from matching profiles will appear here.";return"New matching profiles will appear here."}
